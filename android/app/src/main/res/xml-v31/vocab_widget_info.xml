<?xml version="1.0" encoding="utf-8"?>
<!-- The xml/ copy with the API 31+ attributes added; see there for the rest.

     targetCellWidth/Height are what this file exists for. Without them a widget
     is placed at its minimum, and the minimum here is deliberately tiny so it
     can be squashed — which would otherwise mean every placement started as the
     smallest thing it is allowed to be. Four by two is the size the medium
     bucket is drawn for. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/vocab_widget_description"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:minWidth="110dp"
    android:minHeight="48dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="1800000" />
